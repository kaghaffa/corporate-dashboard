import React from 'react';

const Metrics = React.createClass({
  propTypes: {
  },

  render() {
    return (
      <div className="metrics">
        <h1>Metrics</h1>
      </div>
    );
  }
});

export default Metrics;